apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: mesh-six
  labels:
    app: auth-service
    app.kubernetes.io/part-of: mesh-six
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: auth-service
