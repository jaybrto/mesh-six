apiVersion: v1
kind: Service
metadata:
  name: claude-mqtt-bridge
  namespace: mesh-six
  labels:
    app: claude-mqtt-bridge
    app.kubernetes.io/part-of: mesh-six
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: claude-mqtt-bridge
