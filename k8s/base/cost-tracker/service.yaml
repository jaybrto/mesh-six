apiVersion: v1
kind: Service
metadata:
  name: cost-tracker
  namespace: mesh-six
  labels:
    app: cost-tracker
    app.kubernetes.io/part-of: mesh-six
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: cost-tracker
