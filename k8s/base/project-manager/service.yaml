apiVersion: v1
kind: Service
metadata:
  name: project-manager
  namespace: mesh-six
  labels:
    app: project-manager
    app.kubernetes.io/part-of: mesh-six
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: project-manager
