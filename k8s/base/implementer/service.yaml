apiVersion: v1
kind: Service
metadata:
  name: implementer
  namespace: mesh-six
  labels:
    app: implementer
    app.kubernetes.io/part-of: mesh-six
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: implementer
