apiVersion: v1
kind: Service
metadata:
  name: homelab-monitor
  namespace: mesh-six
  labels:
    app: homelab-monitor
    app.kubernetes.io/part-of: mesh-six
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: homelab-monitor
